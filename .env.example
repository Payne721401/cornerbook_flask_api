# .env.example
# This file lists environment variables used by the application.
# Copy this file to .env and fill in your actual values for local development.
# DO NOT COMMIT YOUR .env FILE TO VERSION CONTROL.

# --- Application Secret Key ---
# Used for session management and security features.
# Generate a long, random string for production.
SECRET_KEY="your_application_secret_key"

# --- Database Configuration (Local PostgreSQL) ---
# Replace with your local PostgreSQL credentials.
DB_USER="your_db_username"
DB_PASSWORD="your_db_password"
DB_HOST="localhost"
DB_PORT="5432"
DB_NAME="your_local_db_name"

# --- API Key for Protected Endpoints ---
# This API key is required for POST, PATCH, DELETE requests.
# Generate a secure key for production and ensure it matches the one set in cPanel.
API_KEY="your_secure_api_key"

# --- Logging Configuration ---
# LOG_LEVEL can be DEBUG, INFO, WARNING, ERROR, CRITICAL.
LOG_LEVEL="INFO"
# Path to the log file for local development. E.g., 'logs/app.log'
# This will be overridden by cPanel settings in production.
LOG_FILE="logs/app.log"
# Set to True for console output during local development.
LOG_TO_STDOUT="True"

# --- Flask Environment (for local development) ---
# Set to 'development' for development features like reloader.
# For production, this will be set to 'production' via cPanel environment variables.
FLASK_ENV="development"
